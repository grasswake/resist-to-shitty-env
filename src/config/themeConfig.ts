import type { ThemeConfig } from 'antd'

export const themeConfig: ThemeConfig = {
  token: {
    // fontSize: 16,
    // colorPrimary: '#52c41a',
    // colorBgContainer: '#52c41a',
  },
  components: {
    Layout: {
      headerHeight: 64,
    },
  },
}
